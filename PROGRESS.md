# 작업 기록

미야코지마 여행 일정 웹. **어디까지 했고 다음에 뭘 하면 되는지** 정리한 문서입니다.
마지막 갱신: 2026-08-17

---

## 한눈에

| | |
|---|---|
| 사이트 | <https://jiakeem04.github.io/mddtrip/> (배포 완료, 동작 중) |
| 저장소 | <https://github.com/jiakeem04/mddtrip> (public) |
| 시트 | `1UH0L0-mp1m2U65_jA8dRkpGV_PSyTErU` — 링크가 있는 모든 사용자 · 뷰어 |
| 여행 | 2026-09-03(목) ~ 09-06(일), 미야코지마. 선발대 6명 + 후발대 2명 |
| 상태 | **4일 27건 전부 정상 표시.** 남은 건 아래 "지금 해야 할 것" 3건 |

여행까지 **약 2주 반** 남았습니다.

---

## 어떻게 굴러가는가

```
Google Sheets ──[브라우저가 gviz CSV를 직접 fetch]──► 정적 페이지 (GitHub Pages)
```

서버도 DB도 빌드도 없습니다. **시트를 고치고 웹을 새로고침하면 바로 반영됩니다.**
저장소를 다시 올릴 필요가 없습니다. 코드·디자인을 고쳤을 때만 `git push` 하면 1~2분 뒤 반영됩니다.

읽는 탭: `00_설정`, `01_일정_Day1`~`Day4`, `03_준비물`
안 읽는 탭: `01_일정`(백업), `02_맛집`, `99_작성가이드`

---

## 🔴 지금 해야 할 것

### 1. `01_일정` 백업 탭에 Klook 바우처 링크가 남아 있음 — 미해결

`01_일정` 탭 **14행**(맹그로브 투어)의 `참고 링크` 칸에 원본 Klook 바우처 URL이 그대로 있습니다.
`01_일정_Day2`는 암호문으로 잘 바꾸셨는데 백업 탭은 안 바뀌어서, **시트가 공개라 아직 노출 상태입니다.**

- 셀 내용 삭제 **+ 셀 우클릭 → 링크 제거** (글자만 지우면 하이퍼링크가 남습니다)
- `01_일정`은 Day1~4로 완전히 대체된 백업이라 **탭째 삭제가 가장 확실합니다**
- ⚠️ 탭 **숨기기로는 안 됩니다.** 숨김은 보기 설정일 뿐이라 숨긴 탭도 링크로 읽힙니다

이 링크는 2026-08-16부터 공개 저장소의 시트 ID를 통해 접근 가능한 상태였습니다.
신경 쓰이면 Klook에서 바우처를 재발급받고, 새 URL로 토큰을 다시 만드세요 (아래 참고).

### 2. 준비물에 국제운전면허증이 없음

`01_일정_Day2` 14:20에 Heaven's 렌터카 픽업이 확정 일정으로 들어가 있는데, `03_준비물`에 없습니다.
일본은 **국제운전면허증 + 국내 운전면허증 원본을 함께** 제시해야 하고 발급에 시간이 걸립니다.

### 3. 비용이 3건뿐이라 예산 화면이 비어 있음

지금 입력된 건 공항버스 ¥600 ×3 뿐입니다. 투어비·렌터카·식사비를 넣으면 예산 화면이 의미를 갖습니다.

---

## 🟡 하면 좋은 것

- **좌표 없는 일정 14건.** 대부분 점심·저녁·샤워라 그대로 둬도 됩니다. 다음 셋만 넣으면 지도가 살아납니다:
  - `맹그로브 투어`(9/4), `야비지 투어`(9/5) — 투어 지점
  - `시모지시마 출발`(9/6) — 9/3 행의 `24.8179819, 125.1324387` 복사
- **`99_작성가이드` 탭을 다시 올리기.** `docs/99_작성가이드.csv`에 공개 범위 경고를 강화했는데 아직 시트에 반영 안 됨.
  (파일 → 가져오기 → 업로드 → **현재 시트 바꾸기**, 대상 탭을 먼저 활성화)
- **커밋 작성자 이메일.** 지금 `jiakeem@kyowon.co.kr`인데 GitHub 계정은 `jiakeem04`입니다.
  프로필에 연결하려면 `git config --local user.email "<GitHub 등록 이메일>"`. 기능엔 영향 없습니다.

---

## ⚪ 보류한 결정

- **시트 주소 노출** → "그대로 두기"로 결정. 대신 README·작성가이드에 경고를 박아둠.
  정적 사이트에선 원리상 숨길 수 없습니다(브라우저가 직접 가져와야 하므로 네트워크 탭에 보임).
  진짜 비공개가 필요해지면 **Apps Script 프록시**가 유일한 해법입니다.
- **PWA 오프라인 캐시** → 도입 안 함. 해외에서 데이터 없이 보려면 나중에 추가 가능.
- **02_맛집을 웹에 노출** → 안 하기로 함. 시트에서만 관리.
- **통화 환산** → 안 함. 시트에 적힌 엔화 그대로 합산.

---

## 지금까지 한 일

**기반** — 구글 시트 gviz CSV를 브라우저에서 직접 읽는 구조. 빌드 없는 순수 HTML/CSS/ES모듈.
Leaflet은 CDN 대신 `vendor/`에 로컬 사본(해외에서 CDN이 느리거나 막힐 수 있어서).

**화면** — 일자별 타임라인 / 지도(핀 + 동선) / 예산 집계 / 준비물 체크리스트.
내용이 없는 화면의 탭은 자동으로 숨겨집니다(지금은 `예약` 탭이 숨겨진 상태).

**구글 지도 연동** — API 키 없이 URL 규격만 사용. 구간별 길찾기 + 하루 전체 동선(경유지 9곳 제한, 초과 시 몇 곳 빠졌는지 표시). 모바일에선 구글 지도 앱으로 열립니다.

**디자인** — 미야코지마 바다 청록 팔레트. 라이트/다크 각각 따로 정의.
카테고리 색은 JS가 아니라 **CSS**에 있습니다(`.c-teal` 같은 클래스). 두 모드의 명암 요구가 반대라서요.
대비는 두 모드 42개 조합 전부 WCAG AA(4.5:1) 통과.

**잠긴 링크** — 개인 정보가 담긴 링크를 AES-GCM으로 암호화. 열쇠는 비밀번호에서 PBKDF2(25만 회)로 생성.
시트에는 `secret:...` 암호문만 들어가고 페이지 소스에도 암호문뿐입니다.

**배포** — GitHub Pages. 실제 도메인에서 CORS 확인 완료(`access-control-allow-origin: https://jiakeem04.github.io`).

---

## 함정 모음 — 다시 만나면 헷갈릴 것들

### gviz `headers=1` 은 데이터를 소리 없이 지운다

**한 번 크게 데인 부분입니다.** gviz에 "첫 행은 헤더"라고 알려주면 남은 행으로 컬럼 타입을 추론하는데,
한 열에 텍스트 셀과 시각 셀이 섞여 있으면 **다른 타입인 셀을 빈 값으로 만들어 돌려줍니다.**
시간 열에서 `08:25`·`11:00`·`16:00`·`19:30`이 사라지고 `12:00`·`13:00`만 남는 식이었습니다.

→ 지금은 **항상 `headers=0`** 으로 받고 헤더는 우리가 직접 처리합니다 (`assets/js/sheets.js`).
이 설정을 되돌리지 마세요.

### 「이동수단」은 "여기에 **오는** 방법"

시트 작성 방식에 코드를 맞췄습니다. 요나하마 비치 행의 `택시`는 *숙소 → 요나하마 비치*를 뜻합니다.
그래서 구간 길찾기는 **도착지 쪽** 값을 씁니다 (`maplinks.js`의 `directionsUrl`).

### `maps.app.goo.gl` 단축링크엔 좌표가 없다

지도 핀을 찍으려면 `좌표` 칸에 `24.7421, 125.2527` 형태로 따로 넣어야 합니다.
단축링크를 푸는 건 브라우저에서 CORS로 막혀 불가능합니다(서버에서 curl로는 됩니다).

### 검색과 경로의 기준이 다르다

「📍 지도」 버튼은 관대합니다(좌표 → 장소 → 제목 순으로 폴백).
구간 길찾기는 엄격합니다(**좌표나 장소가 있어야** 함). '자유시간' 같은 제목 사이에 없는 경로를
있는 것처럼 안내하지 않기 위해서입니다.

### 항공 구간엔 길찾기를 안 붙인다

이동수단에 `비행기`/`항공`이 들어가면 버튼 없이 텍스트만 표시합니다. 구글 지도가 항공 경로를 못 냅니다.

### 숨긴 탭도 읽힌다

탭 숨기기는 보기 설정이지 권한이 아닙니다. 가려야 하면 삭제하거나 다른 문서로 옮기세요.

### 설정 탭은 헤더 없이 읽는다

`00_설정`엔 `키`/`값` 헤더 행이 없습니다. 헤더로 처리하면 첫 줄이 통째로 사라집니다.
`sheets.js`의 `RAW_KINDS`가 이걸 담당합니다. 키의 공백은 무시됩니다(`여행 제목` = `여행제목`).

---

## 작업 방법

### 로컬에서 보기

```bash
python3 -m http.server 8000     # 그다음 http://localhost:8000
```

`file://`로 열면 CORS로 데이터를 못 읽습니다. 반드시 HTTP 서버로 여세요.
`config.js`의 `forceSampleData: true`로 두면 `sample/` 예시 데이터로 뜹니다.

### 검증 (브라우저 없이)

```bash
node tools/check-render.mjs     # 파서·렌더 회귀 117개 (DOM을 흉내 내 실제 실행)
node tools/check-sheet.mjs      # 실제 시트를 읽어 화면에 뭐가 나올지 출력 + 데이터 점검
node tools/check-contrast.mjs   # 라이트/다크 색 대비 WCAG AA 검사
```

색이나 파서를 건드렸으면 세 개 다 돌리세요. 전부 통과해야 정상입니다.

### 링크 잠그기

```bash
echo "https://잠글링크" | node tools/encrypt-link.mjs --password "비밀번호"
# → secret:YHwIbK8Dia...
```

출력된 한 줄을 시트 `관련 링크` 칸에 넣고 **원래 URL은 반드시 지웁니다.**
현재 맹그로브 투어의 비밀번호는 `akteho`입니다.

> `akteho`는 소문자 6자(경우의 수 약 3억)라 마음먹으면 대입으로 뚫립니다.
> 지인 사이 우연한 노출을 막는 수준이지 강한 보안이 아닙니다. 중요해지면 긴 비밀번호로 다시 만드세요.

### 배포

```bash
git add -A && git commit -m "설명" && git push
```

1~2분 뒤 반영됩니다. 시트만 바꿀 때는 이 과정이 필요 없습니다.

---

## 파일 구성

```
index.html              화면 뼈대
config.js               ← 시트 ID·탭 이름. 보통 여기만 고치면 됩니다
assets/style.css        스타일 + 색 토큰 (카테고리 색이 여기 있습니다)
assets/js/
  main.js               부트스트랩, 상태, 날짜 선택, 탭 자동 숨김
  sheets.js             시트 CSV 읽기 + CSV 파서 (headers=0 주의)
  model.js              날짜·시간·좌표·금액·링크 파싱, 날짜별 그룹핑
  timeline.js           타임라인 렌더
  map.js                Leaflet 지도
  maplinks.js           구글 지도 딥링크
  budget.js             예산 집계
  bookings.js           항공·숙소 (현재 시트에 탭 없음)
  packing.js            준비물 체크리스트
  secret.js             잠긴 링크 복호화 + 대화상자
  dom.js                작은 DOM 헬퍼
tools/                  검증 스크립트 + 링크 암호화 도구
vendor/leaflet.*        지도 라이브러리 로컬 사본
sample/*.csv            시트 없이 볼 때 쓰는 예시 데이터
docs/SHEET_TEMPLATE.md  시트 구조 상세 (개발자용)
docs/99_작성가이드.csv   편집자용 안내. 시트 99_작성가이드 탭에 가져오기로 올림
```

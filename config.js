// ─────────────────────────────────────────────────────────────
//  여기만 수정하면 됩니다. 나머지 파일은 건드릴 필요 없습니다.
// ─────────────────────────────────────────────────────────────

export const CONFIG = {
  // 구글 시트 문서 ID.
  // 주소창의 https://docs.google.com/spreadsheets/d/【이 부분】/edit 를 그대로 붙여넣으세요.
  // 비워두면 sample/ 폴더의 예시 데이터로 동작합니다.
  sheetId: '1UH0L0-mp1m2U65_jA8dRkpGV_PSyTErU',

  // 구글 시트 하단의 탭 이름과 "정확히" 일치해야 합니다. (공백·대소문자·오타 주의)
  // 값을 비우면 그 탭은 아예 읽지 않습니다.
  tabs: {
    settings: '00_설정',

    // 일정은 탭 여러 개를 순서대로 읽어 하나로 합칩니다.
    // 아직 안 만든 탭이나 빈 탭은 조용히 건너뜁니다. 날짜가 늘면 여기에 추가하세요.
    // ('01_일정'은 원본 백업이라 읽지 않습니다.)
    itinerary: [
      '01_일정_Day1',
      '01_일정_Day2',
      '01_일정_Day3',
      '01_일정_Day4',
    ],

    packing: '03_준비물',

    // 항공·숙소를 따로 관리하고 싶으면 탭을 만들고 이름을 넣으세요. 없으면 그냥 둡니다.
    bookings: '',
  },

  // (선택) 폴백용 게시 CSV 주소.
  // 파일 > 공유 > 웹에 게시 에서 탭별로 CSV를 골라 나온 주소를 넣습니다.
  // gviz 방식이 실패할 때만 쓰입니다. 안 넣어도 됩니다.
  publishedCsv: {
    settings: '',
    itinerary: '',
    packing: '',
    bookings: '',
  },

  // true 로 두면 sheetId 가 있어도 무조건 sample/ 예시 데이터를 씁니다. (디자인 확인용)
  forceSampleData: false,

  // 브라우저 탭으로 되돌아왔을 때 자동으로 다시 불러올지
  refreshOnFocus: true,

  // 자동 재조회 최소 간격(초). 너무 잦은 요청을 막습니다.
  refreshMinIntervalSec: 30,

  // 지도 기본값. 시트 `00_설정` 탭에 값이 있으면 그쪽이 우선합니다.
  defaultMapCenter: [24.7900, 125.2800], // 미야코지마
  defaultMapZoom: 11,
};
